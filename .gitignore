# === OS / editor junk ===
.DS_Store
Thumbs.db
*.swp
.vscode/
*.iml

# === Java heap/crash dumps (these can be hundreds of MB) ===
*.hprof
hs_err_pid*.log

# === Backend (Maven / Spring Boot) ===
backend/target/
backend/.mvn/
backend/*.log
backend/.idea/
backend/*.iml

# Note: frontend/ and android/ each have their own .gitignore
